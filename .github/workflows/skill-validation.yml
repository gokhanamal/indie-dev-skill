name: Skill Validation

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-skill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate skill structure and frontmatter
        run: .github/scripts/check-skill.sh .
